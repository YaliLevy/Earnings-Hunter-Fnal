{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo PORT=$PORT && python -c \"import api.main; print('Import OK')\" && uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000} --log-level info",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
